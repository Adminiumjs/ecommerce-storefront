// Presentation constants that are neither catalog data nor merchant settings.
//
// `STAR_COLOR` used to live in `data/demo.ts` and was imported by three
// components, which made three screens read the seed for something that is not
// a row and never comes from a database. It is the colour of a filled star.
export const STAR_COLOR = "#f5a623";
